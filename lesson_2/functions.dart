/**
 * Dart is a true object-oriented language,
 * so even functions are objects and have a type, Function.
 * This means that functions can be assigned to variables or
 * passed as arguments to other functions
 *
 * */

/**
 * returnType functionName(parameters) {
 *   // Funksiya tanasi
 *  return value;
 *   }
 * */

/**
 *  function with parameter
 *  void greetUser(String User)
 *  {
 *      print("Hi ${User}")
 *  }
 * */

/**
 * return parameter function
 * User getUser(int UserId){
 * return User();
 * }
 * */

/**
 * Anonim function
 * var myNumber =(int a, int b )=> a*b;
 *
 * */

/**
 * Function o'zgaruvchi
 * Function showMessage=(){
 * print('Bu funksiya o\'zgaruvchi sifatida saqlandi.');
 * }
 * */

/**
 * void execute(Function callback){
 *callback();
 * }
 * */
